<div class="container">
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= image_url 'banner.jpg', alt: "Banner" %>)">
    Our imaginary friends to rent
  </div>
  <div class="d-flex flex-wrap col-sm-12">
    <% @imaginary_friends.each do |friend| %>
      <div class="card-friend">
        <%# if friend.photo.exist? %>
          <%#= image_tag friend.photo, alt: "Imaginary friend" %>
        <%# else %>
          <%= image_tag 'default_friend', alt: "Imaginary friend" %>
        <%# end %>
        <div class="card-friend-infos">
          <div>
            <h2><%= friend.name %></h2>
            <p><%= friend.description %> </p>
            <p><%= link_to "Read more about this imaginary friend", imaginary_friend_path(friend) %></p>
          </div>
          <h2 class="card-friend-pricing"><%= friend.price %> €</h2>
          <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-friend-user avatar-bordered" />
          <%# What is this? %>
          <%# A placeholder for the user avatar which owns this iFriend, remove this feature? %>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to "Add a new friend", new_imaginary_friend_path, class:"btn btn-primary ms-3 mt-2"%>
</div>
